# v0.12.2: 2026-08-29

## Models and Providers

- Added model discovery on the settings page: a discovery dialog, a model picker list, and agent model mappings.
- Simplified the setup wizard around the new discovery flow.

## Oracle

- Added Ctrl+A to the Oracle console and refactored run control, replay, and retrieval for smoother continuation and shutdown.

## Fixes and Reliability

- Fixed a negotiator deal bug and a small ingame-bridge issue.
- When a service fails to start, the launcher now prints the final lines from that service's combined log (`scripts/vox-deorum.cmd`).
